---
title: Aliases
permalink: /aliases.json
layout: null
---
{
{% for post in collections.withAliases %}
  {% if post.data.altUrls %}
    "{{ post.url }}": {{ post.data.altUrls | dump | safe }}{{ "," if not loop.last }}
  {% endif %}
{% endfor %}
}