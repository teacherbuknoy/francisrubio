---
title: Writing Categories - Francis Rubio
description: My blog posts, sorted by categories.
pagination:
  data: collections.postTags
  size: 1
  reverse: true
  alias: category
permalink: /writing/categories/{{ category }}/
activePath: writing
---
<section class="page" aria-labelledby="section-heading-4" style="--page-cover-image: url(https://ik.imagekit.io/8jjzxcl9p/index/cover-pride-musings.png)">
  <header class="page__cover">
    <hgroup role="group" class="page__header">
      <span id="section-heading-4" class="uppercase ls-l font-sans" data-baybayin="{{ sections.writing.baybayin_label }}">{{ sections.writing.label | safe }}</span>
      <h1 class="responsive">{{ sections.writing.title | safe }}</h1>
      <p>{{ sections.writing.description | safe }}</p>
    </hgroup>
  </header>
  <div class="page__inner">
    <div class="tag-scroller">
      {% for item in collections.postTags %}
        <a href="/writing/categories/{{ item }}/" class="tag {{ "active" if category === item }}" style="width: fit-content;">{{ item }}</a>
      {% endfor %}
    </div>
    {% set collection = utils.filterByCategory(collections.posts, category) | reverse %}
    {% include 'components/post-grid.njk' %}
  </div>
</section>