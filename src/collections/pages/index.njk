---
title: Francis Rubio
description: Web developer, designer, and educator from the Philippines
permalink: /
activePath: frontpage
stylesheets: ['frontpage.css', mastodon.css]
scripts: ['frontpage.js', mastodon.js]
---

{% set mastodon = socials.mastodon %}
{% set profile = mastodon.profile %}
{% set feed = mastodon.feed %}
<script>
  window.MASTODON = {
    accountIds: {{ socials.mastodon.accountIds | dump | safe }},
    feed: `{{ socials.mastodon.endpoints.feed | safe}}`,
    profile: `{{ socials.mastodon.profile.feed | safe }}`
  }
</script>

{% include "components/banners/home-banner.njk" %}

<section class="section padding-block--xxl" id="section-2" aria-labelledby="section-2-heading">
  <div class="section__inner">
    <h2 style="width: min(35ch, 100%);" class="fs-4 weight-300" id="section-2-heading">I specialize in creating <strong class="fg-primary-700 weight-bold">accessible front ends</strong> for web projects and integrating them with APIs and back-end processes.</h2>
    <div class="scroll-snapper d-flex gap--m padding--xl">
      {% for feature in features %}
        <div class="feature-card snap-center" style="flex-basis: 30ch; flex-shrink: 0;">
          <button data-popover data-aria-hide data-disclosure class="feature-card__header">{{ feature.title }}</button>
          <div class="feature-card__content">{{ feature.content }}</div>
        </div>
      {% endfor %}
    </div>
    <div class="stack ai--center text-center gap--s padding--xl">
      <span class="fs-4 weight-300">Ready to give me a shot?</span>
      <button class="primary button" data-dialog="modal__email">Connect with me</button>
    </div>
  </div>
</section>
<section class="section padding-block--xxl" id="section-3" aria-labelledby="section-3-heading">
  <div class="section__inner">
    <hgroup role="group" class="stack gap--none">
      <p id="section-3-heading" class="uppercase ls-l font-sans" data-baybayin="{{ sections.experience.baybayin_label }}">
        <span>{{ sections.experience.label | safe }}</span></p>
      <h2 class="fs-1">{{ sections.experience.title | safe }}</h2>
    </hgroup>
    <div class="with-sidebar padding-block--xl">
      <div class="sidebar padding-inline--m" style="flex-basis: 20ch;">
        <div data-tablist style="gap: 0.5ch; display: grid; grid-template-columns: repeat(auto-fit, minmax(4ch, 1fr));" role="tablist">
          {% for tech in technologies %}
            <a aria-label="{{ tech.title }}" href="#tabpanel-{{ tech.icon }}" id="tab-{{ tech.icon }}" data-tab class="button square tab-trigger">
              <svg>
                <title>{{ tech.title }}</title>
                <use href="/assets/images/technologies.svg#{{ tech.icon }}"></use>
              </svg>
            </a>
          {% endfor %}
        </div>
      </div>
      <div class="not-sidebar filled stack gap--xl" id="technologies-tabpanels">
        <div class="tabpanel-container">
          {% for tech in technologies %}
            <section aria-labelledby="{{ tech.icon }}-heading" class="stack tab-panel" id="tabpanel-{{ tech.icon }}">
              <div class="cluster ai--center column-gap--m lh-s margin-bottom--m">
                <span class="uppercase ls-l font-sans">{{ tech.category }}</span>
                <span style="aspect-ratio: 1/1; background-color: hsl(var(--fg-non-essential)); width: 0.6ch; border-radius: 100%;"></span>
                <span class="uppercase ls-xs font-sans">{{ tech.experience }} years of experience</span>
              </div>
              <h3 class="margin--none" id="{{ tech.icon }}-heading">{{ tech.title }}</h3>
              <div class="stack gap--s">
                <p>{{ tech.content | safe }}</p>
              </div>
            </section>
          {% endfor %}
        </div>
        <div class="stack fill-reset ai--center text-center gap--s padding--xl">
          <span class="fs-4 weight-300">Ready to give me a shot?</span>
          <button class="primary button" data-dialog="modal__email">Connect with me</button>
        </div>
      </div>
    </div>
  </div>
</section>
<section class="section padding-block--xxl" id="section-4" aria-labelledby="section-41-heading">
  <div class="section__inner">
    <div class="stack gap--none">
      <hgroup role="group">
        <p id="section-41-heading" class="uppercase ls-l font-sans" data-baybayin="{{ sections.writing.baybayin_label }}">{{ sections.writing.label | safe }}</p>
        <h2 class="fs-1">{{ sections.writing.title | safe }}</h2>
        <p>{{ sections.writing.description | safe }}</p>
      </hgroup>
      {% set postLimit = 4 %}
      {% set collection = collections.posts | reverse %}
      {% include 'components/post-grid.njk' %}
    </div>
    <div class="stack ai--center text-center gap--s padding--xl">
      <div class="cluster center-align">
        <a href="/writing/" class="primary button">See all posts</a>
      </div>
    </div>
  </div>
</section>
{#<section class="section padding-block--xxl" id="section-44" aria-labelledby="section-4-heading">
  <div class="section__inner">
    <div class="stack gap--none">
      <hgroup role="group">
        <p id="section-4-heading" class="uppercase ls-l font-sans"  data-baybayin="{{ sections.literature.baybayin_label }}">{{ sections.literature.label | safe }}</p>
        <h2 class="fs-1">{{ sections.literature.title | safe }}</h2>
        <p>{{ sections.literature.description | safe }}</p>
      </hgroup>
      {% set postLimit = 4 %}
      {% set postType = 'literature' %}
      {% set collection = collections.literature | reverse %}
      {% include 'components/post-grid.njk' %}
    </div>
    <div class="stack ai--center text-center gap--s padding--xl">
      <div class="cluster center-align">
        <a href="/literature/" class="primary button">See all</a>
      </div>
    </div>
  </div>
</section>#}
{# <section class="padding-block--xxl">
  <div class="section__inner">
    <aside class="callout callout--success">
      {{ 'gift' | icon | safe }}
      <p class="callout__body">
        <strong>Feature rollout!</strong> I am rolling out my favorite threads that I posted somewhere else. Take a look at them!</p>
      <div class="callout__footer">
        <a href="/socials/mastodon/featured/" class="button button--success">See featured threads</a>
      </div>
    </aside>
  </div>
</section> #}
<section class="padding-block--xxl" id="section-5" aria-labelledby="section-5-heading">
  <div class="section__inner">
    <hgroup role="group" class="stack gap">
      <p id="section-5-heading" class="uppercase ls-l font-sans"  data-baybayin="{{ sections.talks.baybayin_label }}">{{ sections.talks.label | safe }}</p>
      <h2 class="fs-1">{{ sections.talks.title | safe }}</h2>
      <p>{{ sections.talks.description | safe }}</p>
    </hgroup>
    <div class="margin-block--l">
      {% set postLimit = 3 %}
      {% set hasFeatured = false %}
      {% set isReversed = true %}
      {% set collection = collections.talks | reverse %}
      {% include 'components/talks-grid.njk' %}
    </div>
    <div class="stack ai--center text-center gap--s padding--xl">
      <span class="fs-4 weight-300">Have an event coming up?</span>
      <div class="cluster center-align center-justified">
        <a href="/speaking/" class="button">See all of my previous talks</a>
        <a href="/speaker-rider/" class="primary button">Invite me as a speaker</a>
      </div>
    </div>
  </div>
</section>
<section class="padding-block--xxl" id="section-56" aria-labelledby="section-6-heading">
  <div class="section__inner">
    <hgroup role="group" class="stack gap">
      <p id="section-6-heading" class="uppercase ls-l font-sans"  data-baybayin="{{ sections.projects.baybayin_label }}">Projects</p>
      <h2 class="fs-1">The Gallery</h2>
      <p>My most notable projects. Things that I am proud I made, or was involved in making. Work that I would love to do more of.</p>
    </hgroup>
    {% set postLimit = 3 %}
    {% set collection = collections.projects | reverse %}
    <span class="deck-card-container m-block-start--xxxl">
      {% set limit = postLimit if collection.length > postLimit else 
        collection.length %}
      {% for i in range(0, limit) %}
        {% set item = collection[i] %}
        {% include 'components/deck-card.njk' %}
      {% endfor %}
    </span>
    <div class="stack ai--center text-center gap--s padding--xl">
      <span class="fs-4 weight-300">Building a similar project?</span>
      <div class="cluster center-align center-justified">
        <a href="/projects/" class="button">See all of my projects</a>
        <button class="primary button" data-dialog="modal__email">Bring me to your team</button>
      </div>
    </div>
  </div>
</section>
<section class="padding-block--xxl bg--secondary" id="section-6" aria-labelledby="section-61-heading">
  <div class="section__inner jc--center stack gap text-center">
    <p id="section-61-heading" class="uppercase ls-l font-sans"  data-baybayin="{{ sections.threads.baybayin_label }}">{{ sections.threads.label | safe }}</p>
    <h2 class="fs-1">{{ sections.threads.title | safe }}</h2>
    <p>{{ sections.threads.description | safe }}</p>
    <script>
      window.MASTODON.threads = {{ mastodon.feed | removeReblogs | removeReplies | dump | safe }}
    </script>
    <div class="profile__feed profile__feed--scroll">
      {% for thread in mastodon.feed | removeReblogs | removeReplies %}
        {% if loop.index0 < 6 %}
          {% set entry = thread %}
          {% set disableMedia = true %}
          {% set noreply = true %}
          {% include "components/mastodon-feed-entry.njk" %}
        {% endif %}
      {% endfor %}
    </div>
    <div class="cluster jc--center padding">
      <a href="/socials/mastodon/" class="primary button">See all of my Mastodon posts</a>
      <a href="/socials/mastodon/" class="button">See my featured Mastodon posts</a>
    </div>
  </div>
</section>
<section class="section padding-block--xxl" id="section-44a" aria-labelledby="section-4a-heading">
  <div class="section__inner">
    <div class="stack gap--none">
      <hgroup role="group">
        <p id="section-4a-heading" class="uppercase ls-l font-sans" data-baybayin="{{ sections.videos.baybayin_label }}">{{ sections.videos.label | safe }}</p>
        <h2 class="fs-1">{{ sections.videos.title | safe }}</h2>
        <p>{{ sections.videos.description | safe }}</p>
      </hgroup>
    </div>
    {% set postLimit = 3 %}
    {% set collection = collections.videos | reverse %}
    {% include 'components/video-grid.njk' %}
    <div class="stack ai--center text-center gap--s padding--xl">
      <div class="cluster center-align">
        <a href="/videos/" class="primary button">See all</a>
      </div>
    </div>
  </div>
</section>
<section class="section padding-block--xxl bg-secondary" id="footer" aria-labelledby="footer-heading">
  <div class="section__inner">
    <div class="stack gap">
      <hgroup role="group">
        <p id="footer-heading" class="uppercase ls-l font-sans"  data-baybayin="{{ sections.contact.baybayin_label }}">{{ sections.contact.label | safe }}</p>
        <h2 class="fs-1">{{ sections.contact.title | safe }}</h2>
      </hgroup>
      <p style="width: min(75ch, 100%)">{{ sections.contact.description | safe }}</p>
    </div>
    <div class="cluster gap--m padding--xl center-justified">
      {% for key, value in site.contact %}
        <a target="_blank" rel="noopener me" href="{{ value.url }}" class="button">{{ value.name }}</a>
      {% endfor %}
    </div>
  </div>
</section>
<section class="padding-block--xxl bg-secondary" id="comments" aria-labelledby="comments-heading">
  <div class="section__inner">
    <hgroup class="stack-gap" role="group">
      <p id="comments-heading" class="uppercase ls-l font-sans" data-baybayin="{{ sections.webmentions.baybayin_label }}">{{ sections.webmentions.label | safe }}</p>
      <h2 class="fs-1">{{ sections.webmentions.title | safe }}</h2>
    </hgroup>
    <p style="width: min(75ch, 100%)">{{ sections.contact.description | safe }}</p>
    {% include 'components/webmentions.njk' %}
  </div>
</section>

{% include 'components/email-form.njk' %}

{% set preferredDomain = site.domains | selectattr('isPreferred') | first %}
<script type="application/ld+json">
  [{% include "components/seo/structure/website.njk" %}]
</script>